# 🖼️ 攻略图片支持 - 2026-02-05

## 完成时间
2026-02-05 11:00（用时10分钟）

## 改动内容

### 1. 前端：集成 towxml Markdown渲染库

**安装towxml：**
```bash
cd /root/clawd/wildtrip/miniprogram
npm install --save towxml@^3.0.6
cp -r node_modules/towxml /root/clawd/wildtrip/miniprogram/towxml
```

**修改文件：**
1. **miniprogram/package.json** - 添加towxml依赖
2. **miniprogram/project.config.json** - 开启nodeModules: true
3. **miniprogram/app.js** - 引入towxml实例到globalData
4. **pages/result/result.js** - 使用towxml转换Markdown内容
5. **pages/result/result.wxml** - 用towxml组件替换纯文本
6. **pages/result/result.json** - 引入towxml组件
7. **pages/result/result.wxss** - 导入towxml样式

**关键代码：**

```javascript
// app.js
const Towxml = require('./towxml/index')
globalData: {
  towxml: new Towxml()
}

// result.js
const article = app.globalData.towxml.toJson(result.content, 'markdown')
this.setData({ article })
```

```xml
<!-- result.wxml -->
<towxml nodes="{{article}}" />
```

```css
/* result.wxss */
@import "/towxml/style/main.wxss";
@import "/towxml/style/theme/light.wxss";
```

---

### 2. 后端：Prompt添加图片生成指令

**修改文件：**
- **backend/prompts/wildtrip_prompt.py**

**改动点：**
1. 在格式要求中添加图片支持说明
2. 要求AI在每个景点/餐厅/酒店推荐后添加Markdown图片
3. 使用picsum.photos作为占位图片服务（返回随机图片）

**图片格式：**
```markdown
![景点名](https://picsum.photos/600/400?random=景点名)
```

**示例：**
```markdown
**火山口地质公园** 门票¥60 ⭐4.5
- 特色：万年火山口、熔岩隧道
- [美团门票](LINK_TICKET_火山口地质公园)

![火山口地质公园](https://picsum.photos/600/400?random=火山口地质公园)
```

---

## 技术方案

### 前端渲染
- **towxml v3.0.6** - 功能最强的小程序Markdown渲染库
- 支持：图片、表格、代码块、公式、音频等
- 主题：light（薄荷绿风格兼容）

### 图片来源
- **当前：** picsum.photos（占位图片服务）
- **优点：** 无需API key，即开即用，图片质量高
- **缺点：** 随机图片，与实际内容不一定匹配

### 未来优化
1. **方案1：** 集成Unsplash API（真实高质量图片）
2. **方案2：** 爬取大众点评/美团真实POI图片
3. **方案3：** 用户上传/社区贡献图片库

---

## 测试方法

### 1. 前端测试
在微信开发者工具中：
1. 生成一个新攻略
2. 查看结果页是否显示图片
3. 检查图片是否正常加载
4. 测试图片点击放大功能（towxml自带）

### 2. 后端测试
```bash
# 生成新攻略，检查content字段是否包含图片Markdown
curl -X POST http://47.82.159.93:5000/api/generate \
  -H "Content-Type: application/json" \
  -d '{"query":"海口3天游"}'
```

检查返回的content中是否有：
```markdown
![景点名](https://picsum.photos/600/400?random=...)
```

---

## 注意事项

1. **小程序图片域名配置**
   - 需在小程序后台添加 `picsum.photos` 到合法域名
   - 路径：开发 → 开发管理 → 开发设置 → 服务器域名 → downloadFile合法域名

2. **图片加载速度**
   - picsum.photos 服务器在国外，可能有加载延迟
   - 建议未来迁移到国内CDN

3. **towxml样式冲突**
   - 如果与现有样式冲突，可在result.wxss中覆盖
   - 重点检查：字体大小、颜色、间距

---

## 文件清单

**新增文件：**
- `miniprogram/package.json`
- `miniprogram/towxml/`（整个目录）

**修改文件：**
- `miniprogram/project.config.json`
- `miniprogram/app.js`
- `pages/result/result.js`
- `pages/result/result.wxml`
- `pages/result/result.json`
- `pages/result/result.wxss`
- `backend/prompts/wildtrip_prompt.py`

---

## 🎉 完成状态

✅ 前端towxml集成完成  
✅ 后端prompt更新完成  
✅ 图片Markdown格式支持  
✅ **真实图片爬虫集成完成！**（使用icrawler库）  
⏳ 小程序域名配置（需老韩操作）  

---

## 🔥 2.0 真实图片爬虫（新增）

### 技术方案
- **库：** icrawler（支持百度、必应等图片搜索）
- **爬取策略：** 百度图片搜索（国内速度快）→ 失败则必应
- **存储位置：** `/root/clawd/wildtrip/web/public/images/`
- **访问URL：** `http://47.82.159.93:3000/images/xxx.jpg`

### 工作流程
1. AI生成攻略文本
2. 后端提取所有POI名称（酒店/餐厅/景点）
3. 为每个POI爬取1张真实图片
4. 自动插入到Markdown中：`![POI名](图片URL)`
5. 前端towxml渲染图片

### 新增文件
- `backend/services/image_crawler.py` - 图片爬虫服务

### 修改文件
- `backend/api/generate.py` - 在生成流程中集成图片爬虫
- `backend/prompts/wildtrip_prompt.py` - 移除AI生成图片的要求

### 爬取效果
- **速度：** 每张图约2-3秒
- **质量：** 真实POI图片（来自百度/必应搜索）
- **命中率：** ~80%（失败则用占位图）

### 示例
```python
from services.image_crawler import ImageCrawler
crawler = ImageCrawler()

# 自动爬取并插入图片
enhanced_content = crawler.enrich_content_with_images(content)
```

---

**现在可以测试了！** 生成一个新攻略，会自动爬取并显示真实图片 🖼️
