# 小程序订阅消息部署指南

## ✅ 已完成的功能

1. ✅ 小程序订阅页面 (`miniprogram/pages/subscribe/`)
2. ✅ 后端订阅管理 API (`backend/api/subscription.py`)
3. ✅ 定时推送服务 (`backend/services/weekend_push_service.py`)
4. ✅ 集成到主应用 (`backend/app.py`)

---

## 📋 部署步骤

### 第一步: 配置环境变量

在 `backend/.env` 文件中添加:

```bash
# 微信小程序配置
WECHAT_APPID=你的小程序AppID
WECHAT_SECRET=你的小程序AppSecret

# 订阅消息模板 ID(申请后填入)
WECHAT_SUBSCRIBE_TEMPLATE_WEEKEND=
```

**获取 AppID 和 AppSecret:**
1. 登录 https://mp.weixin.qq.com
2. 开发 → 开发管理 → 开发设置
3. 复制 AppID 和 AppSecret

---

### 第二步: 申请订阅消息模板

参考 `小程序订阅消息申请指南.md`

**快速链接:**
https://mp.weixin.qq.com/wxamp/newtmpl/mytmpl

**申请后:**
1. 获取模板 ID
2. 填入 `.env` 文件的 `WECHAT_SUBSCRIBE_TEMPLATE_WEEKEND`

---

### 第三步: 在小程序代码中配置模板 ID

编辑 `miniprogram/pages/subscribe/subscribe.js`

```javascript
data: {
  tmplIds: [
    '你申请的模板ID',  // 替换这里
  ],
  ...
}
```

或者使用环境变量(推荐):
```javascript
tmplIds: [
  process.env.WECHAT_SUBSCRIBE_TEMPLATE_WEEKEND || '',
],
```

---

### 第四步: 小程序端配置跳转路由

在 `miniprogram/app.json` 中添加订阅页面:

```json
{
  "pages": [
    "pages/index/index",
    ...
    "pages/subscribe/subscribe"  // 添加这一行
  ]
}
```

---

### 第五步: 在合适的地方引导用户订阅

#### 方式 1: 生成攻略后引导

在 `miniprogram/pages/result/result.js` 的 `onLoad()` 里添加:

```javascript
onLoad(options) {
  // ... 原有代码
  
  // 🔥 检查是否需要引导订阅
  this.checkAndGuideSubscribe()
},

checkAndGuideSubscribe() {
  const hasSubscribed = wx.getStorageSync('has_subscribed')
  
  if (!hasSubscribed) {
    // 第一次生成攻略,引导订阅
    wx.showModal({
      title: '订阅周末野游计划',
      content: '每周四晚 8 点,为您推送个性化的周末出游推荐。',
      confirmText: '去订阅',
      cancelText: '稍后',
      success: (res) => {
        if (res.confirm) {
          wx.navigateTo({
            url: '/pages/subscribe/subscribe'
          })
        }
      }
    })
  }
}
```

#### 方式 2: 在"我的"页面添加入口

在 `miniprogram/pages/user/user.wxml` 添加:

```xml
<view class="menu-item" bindtap="goToSubscribe">
  <text class="icon">🔔</text>
  <text class="label">订阅周末推送</text>
  <text class="arrow">></text>
</view>
```

```javascript
// user.js
goToSubscribe() {
  wx.navigateTo({
    url: '/pages/subscribe/subscribe'
  })
}
```

---

### 第六步: 后端部署

#### 安装依赖

```bash
cd backend
pip install apscheduler
```

#### 启动服务

```bash
python app.py
```

**查看日志确认:**
```
✅ 周末推送服务已启动(每周四 20:00)
```

---

### 第七步: 测试推送

#### 方式 1: 手动触发测试

在 Python 控制台:

```python
from services.weekend_push_service import manual_push_test

# 替换成你的 openid(从小程序获取)
manual_push_test('oXXXXXXXXXXXXXXX')
```

#### 方式 2: 修改定时任务时间

临时修改 `weekend_push_service.py`:

```python
# 改成 1 分钟后执行(测试用)
self.scheduler.add_job(
    func=self.push_weekend_plans,
    trigger=CronTrigger(hour='*', minute='*/1'),  # 每分钟
    ...
)
```

重启服务,1 分钟后查看日志。

---

## 🧪 测试清单

### 小程序端测试

- [ ] 打开订阅页面,UI 正常显示
- [ ] 点击"立即订阅",弹出微信订阅弹窗
- [ ] 同意订阅,显示"订阅成功"提示
- [ ] 拒绝订阅,显示相应提示
- [ ] 重新进入订阅页,显示"已订阅"状态

### 后端测试

- [ ] 访问 `/api/user/subscription/status?openid=xxx`,返回订阅状态
- [ ] 手动触发推送,用户收到订阅消息
- [ ] 消息点击,跳转到小程序指定页面
- [ ] 访问 `/api/subscription/stats`,查看订阅统计

### 定时任务测试

- [ ] 周四 20:00 自动推送
- [ ] 日志记录推送结果
- [ ] 推送失败有错误日志

---

## 🐛 常见问题

### 1. 用户收不到消息

**可能原因:**
- 用户没有订阅
- access_token 过期或错误
- 模板 ID 错误
- 消息内容超过字数限制(每个字段最多 20 字)

**排查:**
```python
# 查看日志
tail -f backend/logs/wildtrip.log | grep "推送"
```

### 2. 订阅弹窗不出现

**可能原因:**
- 模板 ID 未配置
- 小程序未发布或未审核通过

**解决:**
- 检查 `tmplIds` 是否填写
- 使用开发版/体验版小程序测试

### 3. access_token 错误

**可能原因:**
- AppID 或 AppSecret 配置错误
- 小程序未发布

**解决:**
```bash
# 测试获取 token
curl "https://api.weixin.qq.com/cgi-bin/token?grant_type=client_credential&appid=你的AppID&secret=你的Secret"
```

---

## 📊 监控和优化

### 查看订阅数据

```bash
curl http://localhost:5000/api/subscription/stats
```

### 查看推送日志

```bash
# 最近的推送记录
grep "推送成功" backend/logs/wildtrip.log | tail -20

# 推送失败的记录
grep "推送失败" backend/logs/wildtrip.log | tail -20
```

### 优化建议

1. **推送时间优化**
   - 根据用户活跃时间调整推送时间
   - 考虑分批推送(避免瞬间大量请求)

2. **个性化优化**
   - 接入 Multi-Agent 系统
   - 根据用户偏好生成不同推荐

3. **数据持久化**
   - 迁移到数据库(PostgreSQL/MySQL)
   - 记录推送历史和转化数据

---

## 🚀 下一步优化

- [ ] 接入 Multi-Agent 系统生成个性化推荐
- [ ] 添加推送效果分析(打开率、转化率)
- [ ] 支持用户自定义推送时间
- [ ] 添加推送预览功能
- [ ] 实现 A/B 测试(不同推送内容对比)

---

**代码已全部提交到 GitHub!** 🎉

有问题随时问我!
