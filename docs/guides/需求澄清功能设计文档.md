# 野游记 - 需求澄清功能设计文档 🎯

## 📋 功能概述

**目标：** 通过AI智能提问，引导用户补充关键信息，提升攻略质量。

**核心价值：**
- ✅ 降低用户输入门槛（简单描述即可）
- ✅ 提高攻略精准度（信息更完整）
- ✅ 增强交互体验（对话式引导）
- ✅ 提升转化率（更懂用户需求）

---

## 🎬 用户流程

```
┌─────────────────┐
│  1. 首页输入     │
│  "海口3天亲子游" │
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│  2. AI分析需求   │
│  识别缺失信息    │
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│  3. 展示问题     │
│  Q1: 预算？      │
│  Q2: 人数？      │
│  Q3: 住宿？      │
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│  4. 用户选择     │
│  [舒适] [3-4人]  │
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│  5. 生成攻略     │
│  基于完整信息    │
└─────────────────┘
```

---

## 🏗️ 技术架构

### 前端（小程序）

```
pages/
├── index/          # 首页（修改跳转逻辑）
├── clarify/        # 🆕 需求澄清页
│   ├── clarify.wxml
│   ├── clarify.wxss
│   ├── clarify.js
│   └── clarify.json
└── generate/       # 生成页（保持不变）
```

### 后端（Python + Flask）

```
backend/
├── services/
│   └── need_clarifier.py   # 🆕 需求澄清服务
├── api/
│   └── clarify.py          # 🆕 澄清API蓝图
└── app.py                   # 注册新蓝图
```

### API接口

```
POST /api/analyze-need
  Request:  { "query": "海口3天亲子游" }
  Response: { "questions": [...], "original_query": "..." }

POST /api/generate-with-answers
  Request:  { "original_query": "...", "answers": {...} }
  Response: { "task_id": "...", "status": "processing" }
```

---

## 📊 数据结构

### 1. 问题格式

```json
{
  "id": "budget",
  "question": "您的预算范围是？",
  "type": "single_choice",
  "required": true,
  "options": [
    {
      "value": "budget_low",
      "label": "经济实惠",
      "icon": "💰",
      "description": "¥1000-2000",
      "price_range": [1000, 2000]
    }
  ]
}
```

### 2. 用户回答

```json
{
  "original_query": "海口3天亲子游",
  "answers": {
    "budget": "budget_mid",
    "people_count": "3-4",
    "hotel_level": "comfort"
  }
}
```

---

## 🎨 UI设计要点

### 1. 视觉层次

```
┌────────────────────────┐
│ 📝 您的需求            │
│ "海口3天亲子游"        │
└────────────────────────┘
         ↓
┌────────────────────────┐
│ 🤖 AI提示              │
│ "为了生成更精准的攻略…"│
└────────────────────────┘
         ↓
┌────────────────────────┐
│ Q1 您的预算范围是？[必填]│
│  💰 经济实惠  ¥1000-2000│
│  💎 舒适出游  ¥2000-5000│ ← 选中状态
│  👑 品质优先  ¥5000+    │
└────────────────────────┘
         ↓
┌────────────────────────┐
│ [跳过] [确认 (2/3)]     │
└────────────────────────┘
```

### 2. 交互反馈

- ✅ 选中效果：背景色 + 边框 + ✓图标
- ✅ 禁用状态：灰色 + opacity: 0.5
- ✅ 点击反馈：scale(0.98) 动画
- ✅ 进度提示："已回答 2/3"

### 3. 色彩方案

- **主色调:** #20B2AA（薄荷绿）
- **选中态:** rgba(32, 178, 170, 0.1) 背景
- **必填标签:** #FF3B30（红色）
- **AI气泡:** 渐变 #E0F7F6 → #FFFFFF

---

## 🔧 实现步骤

### Phase 1: 后端实现（30分钟）

```bash
# 1. 创建服务文件
cd backend/services
vi need_clarifier.py  # 复制代码

# 2. 创建API文件
cd ../api
vi clarify.py  # 复制代码

# 3. 注册蓝图
cd ..
vi app.py  # 添加 register_blueprint(clarify_bp)

# 4. 测试API
python app.py
curl -X POST http://localhost:5000/api/analyze-need \
  -H "Content-Type: application/json" \
  -d '{"query":"海口3天亲子游"}'
```

### Phase 2: 前端实现（40分钟）

```bash
# 1. 创建页面目录
cd miniprogram/pages
mkdir clarify
cd clarify

# 2. 创建4个文件
touch clarify.wxml clarify.wxss clarify.js clarify.json
# 复制对应代码

# 3. 修改首页
vi ../index/index.js  # 修改onGenerate方法

# 4. 更新API工具
vi ../../utils/api.js  # 添加新方法

# 5. 注册页面
vi ../../app.json  # 添加 clarify 页面
```

### Phase 3: 测试验证（20分钟）

```
1. 启动后端: python backend/app.py
2. 打开微信开发者工具
3. 编译小程序
4. 测试流程:
   - 输入 "海口3天亲子游"
   - 点击生成按钮
   - 查看澄清页是否正常显示
   - 选择几个选项
   - 点击确认生成
   - 验证攻略质量
```

---

## 🎯 智能提问策略

### 问题优先级（最多5个问题）

1. **预算** ⭐⭐⭐⭐⭐ 必问（影响所有推荐）
2. **人数** ⭐⭐⭐⭐⭐ 必问（影响酒店、餐厅选择）
3. **住宿** ⭐⭐⭐⭐ 常问（预算未明确时）
4. **美食** ⭐⭐⭐ 可选（根据目的地）
5. **交通** ⭐⭐⭐ 可选（距离>100km时）
6. **特殊需求** ⭐⭐ 可选（亲子/老人/情侣）

### 智能判断规则

```python
# 1. 检测预算
if '预算' in query or re.search(r'\d+元', query):
    # 已提供预算，跳过此问题
    pass
else:
    # 必须询问预算
    questions.append(QUESTION_TEMPLATES['budget'])

# 2. 检测人数
if '亲子' in query:
    # 推断3-4人，但仍需确认
    questions.append(QUESTION_TEMPLATES['people_count'])
    # 设置默认值
    default_answers['people_count'] = '3-4'

# 3. 检测目的地特色
if city in ['成都', '重庆', '西安']:
    # 美食城市，必问美食偏好
    questions.append(QUESTION_TEMPLATES['food_preference'])
```

---

## 📈 预期效果

### 用户体验提升

- **输入门槛降低 50%**
  - 之前："海口3天亲子游，预算5000，3大1小，喜欢海鲜..."
  - 现在："海口3天亲子游" → 点几下选项即可

- **攻略质量提升 30%**
  - 信息完整度从 60% → 90%
  - 推荐准确度从 70% → 85%

- **生成成功率提升 20%**
  - 减少因信息不足导致的失败

### 业务指标

- **平均对话轮数:** 1.5轮（原来需要2-3轮）
- **用户满意度:** 预计提升 25%
- **转化率:** 预计提升 15%（更精准推荐）

---

## ⚠️ 注意事项

### 1. 性能优化

- 问题数量控制在 3-5 个
- API响应时间 < 500ms
- 缓存常见问题模板

### 2. 用户体验

- 提供"跳过"选项（不强制）
- 必填题标识清晰
- 进度提示明确

### 3. 兼容性

- 保留原有直接生成功能
- API失败时降级处理
- 支持快速跳过

---

## 🚀 部署清单

### 后端部署

- [ ] 创建 `need_clarifier.py`
- [ ] 创建 `clarify.py`
- [ ] 注册蓝图到 `app.py`
- [ ] 测试API接口
- [ ] 部署到生产服务器

### 前端部署

- [ ] 创建 `clarify` 页面（4个文件）
- [ ] 修改首页跳转逻辑
- [ ] 更新 `api.js` 添加新方法
- [ ] 注册页面到 `app.json`
- [ ] 测试完整流程
- [ ] 提交代码审核

### 测试验证

- [ ] 单元测试（后端API）
- [ ] 集成测试（完整流程）
- [ ] 用户测试（真实场景）
- [ ] 性能测试（响应时间）

---

## 📞 联系方式

**开发者:** Claude Code  
**文档版本:** v1.0  
**更新日期:** 2026-02-14

---

**准备好让 Claude Code 开始实现了吗？** 🚀

按照这个文档，他可以在 1-2 小时内完成全部开发工作！
