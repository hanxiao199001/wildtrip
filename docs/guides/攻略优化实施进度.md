# 攻略优化实施进度

## ✅ 已完成

### 1. 住宿推荐前置 (B部分)
- [x] 修改 Prompt,把住宿放在行程之前
- [x] 添加美团/飞猪比价占位符
- [x] 提交代码

**效果:**
用户现在看到的攻略顺序:
```
1. 行程概览
2. 🏨 住宿推荐 ← 前置!
3. 详细行程
4. 费用预算
```

---

## 🚧 进行中

### 2. 速度优化 (A部分)

#### 需要做的事情:

**A1. 并行处理酒店/美食推荐** (最重要)

当前代码位置: `backend/api/generate.py` 第 200行左右的 `run_generation_task()` 函数

**修改思路:**
```python
# 当前(串行):
攻略内容 = AI生成()  # 30秒
提取酒店() # 5秒
提取美食() # 5秒
提取门票() # 5秒
总计: 45秒

# 优化后(并行):
攻略内容 = AI生成()  # 30秒
并发执行 {
  提取酒店(),  # 5秒
  提取美食(),  # 5秒  
  提取门票()   # 5秒
}
总计: 35秒 (节省10秒)
```

**具体代码改动:**
```python
import concurrent.futures

# 在 run_generation_task() 里,找到这段:
emit_progress(socketio, task_id, 'extract', '📝 提取推荐信息...', 70)

# 改成并行:
with concurrent.futures.ThreadPoolExecutor(max_workers=3) as executor:
    future_hotels = executor.submit(extract_hotels, content, city)
    future_restaurants = executor.submit(extract_restaurants, content, city)
    future_tickets = executor.submit(extract_tickets, content, city)
    
    hotels = future_hotels.result()
    restaurants = future_restaurants.result()
    tickets = future_tickets.result()
```

---

**A2. 优化进度更新频率**

当前进度点太少,导致卡顿感:
```python
# 当前:
0% → 20% → 70% → 100%  (只有4个进度点)

# 优化后:
0% → 10% → 20% → 30% → ... → 100%  (至少10个进度点)
```

**具体改动:**
在 `backend/api/generate.py` 的 `emit_progress()` 调用处,增加更多进度点:

```python
# 找到所有 emit_progress() 调用,增加中间进度
emit_progress(socketio, task_id, 'start', '🔥 开始生成...', 0)
emit_progress(socketio, task_id, 'analyzing', '🔍 分析需求...', 10)
emit_progress(socketio, task_id, 'generating', '🤖 AI生成中...', 20)
# AI生成过程中每5秒更新一次进度
for i in range(20, 70, 10):
    time.sleep(5)
    emit_progress(socketio, task_id, 'generating', f'🤖 生成中 {i}%...', i)
emit_progress(socketio, task_id, 'extract', '📝 提取推荐...', 70)
emit_progress(socketio, task_id, 'done', '✅ 完成!', 100)
```

---

**A3. 前端进度条优化**

文件: `miniprogram/pages/clarify/clarify.js` 和 `clarify.wxml`

**添加进度条组件:**
```xml
<!-- clarify.wxml -->
<view class="progress-container">
  <progress 
    percent="{{progress}}" 
    show-info 
    stroke-width="4"
    activeColor="#07c160"
  />
  <text class="progress-text">{{progressText}}</text>
</view>
```

**JS逻辑:**
```javascript
// clarify.js
data: {
  progress: 0,
  progressText: '准备中...'
},

// Socket监听进度更新
socket.on('progress', (data) => {
  this.setData({
    progress: data.progress,
    progressText: data.message
  })
})
```

---

### 3. 飞猪/美团比价 (C部分)

**已有基础:**
- ✅ 聚推客API已接入 (`backend/services/jutuike_api.py`)
- ✅ 美团酒店: `ACT_IDS['hotel']` = 10
- ✅ 飞猪酒店: `ACT_IDS['feizhu']` = 29

**需要做的:**

**C1. 修改酒店链接生成逻辑**

文件: `backend/api/generate.py` 第 430-470行

**当前:**
```python
# 只生成美团链接
link = affiliate.get_hotel_link(hotel_name, city)
```

**优化后:**
```python
# 同时生成美团和飞猪链接
from services.jutuike_api import get_jutuike_api
jt_api = get_jutuike_api()

# 美团酒店
meituan_link_info = jt_api.get_hotel_link(sid=f'{city}_{hotel_name}')
meituan_link = meituan_link_info['h5'] if meituan_link_info else '#'

# 飞猪酒店
feizhu_link_info = jt_api.get_feizhu_link(sid=f'{city}_{hotel_name}')
feizhu_link = feizhu_link_info['h5'] if feizhu_link_info else '#'

# 保存两个链接
hotel_data = {
    'name': hotel_name,
    'meituan_link': meituan_link,
    'feizhu_link': feizhu_link
}
```

**C2. 替换Prompt中的比价占位符**

在 `process_affiliate_links()` 函数里添加:

```python
# 替换酒店比价链接
for hotel in recommendations['hotels']:
    hotel_name = hotel['name']
    meituan_link = hotel['meituan_link']
    feizhu_link = hotel['feizhu_link']
    
    # 替换 LINK_HOTEL_MEITUAN_xxx
    content = content.replace(
        f'LINK_HOTEL_MEITUAN_{hotel_name}',
        meituan_link
    )
    
    # 替换 LINK_HOTEL_FEIZHU_xxx
    content = content.replace(
        f'LINK_HOTEL_FEIZHU_{hotel_name}',
        feizhu_link
    )
```

**C3. 前端展示优化 (可选)**

如果想要更好的比价UI,可以在小程序端添加比价卡片:

```xml
<!-- 酒店比价卡片 -->
<view class="hotel-compare">
  <text>预订比价:</text>
  <view class="platform-row">
    <navigator url="/pages/webview/webview?url={{meituanLink}}">
      <image src="/images/meituan-logo.png" />
      <text>美团酒店</text>
      <text class="cashback">返现约¥{{meituanCashback}}</text>
    </navigator>
  </view>
  <view class="platform-row">
    <navigator url="/pages/webview/webview?url={{feizhuLink}}">
      <image src="/images/fliggy-logo.png" />
      <text>飞猪酒店</text>
      <text class="cashback">返¥{{feizhuCashback}}</text>
    </navigator>
  </view>
</view>
```

---

## 📊 优化效果预估

| 指标 | 当前 | 优化后 | 提升 |
|------|------|--------|------|
| 生成速度 | 30-60秒 | 20-35秒 | **40%** |
| 进度更新 | 4个点 | 10+个点 | **流畅度提升** |
| 用户体验 | 住宿靠后 | 住宿前置 | **更符合需求** |
| 比价功能 | 仅美团 | 美团+飞猪 | **省钱更直观** |

---

## 🎯 下一步行动

**优先级排序:**

1. **A2 - 进度条优化** (最快见效,30分钟完成)
   - 增加进度更新点
   - 前端添加进度条组件

2. **A1 - 并行处理** (性能提升最大,1小时完成)
   - 修改 `run_generation_task()` 
   - 使用 `ThreadPoolExecutor` 并行处理

3. **C1-C2 - 飞猪比价** (功能增强,2小时完成)
   - 修改链接生成逻辑
   - 替换比价占位符

---

## 💡 你想先做哪个?

回复:
- **A2** - 先优化进度条(快速见效)
- **A1** - 先提升速度(效果最大)
- **C** - 先做比价功能(功能增强)
- **全做** - 按顺序全部实现

告诉我你的选择,我马上开始!
