# 2026-02-19 日记

## 今天做的事

### 野游记 - 美团跳转优化
- 问题：点"美团团购"后跳转美团小程序，还需要手动粘贴城市和地址
- 修复1：跳转前自动 setClipboardData 复制城市名，先设好剪贴板再跳转（修复异步竞态）
- 修复2：合并远端更好的版本（从relay URL解析keyword+city参数），兜底用 guide.destination
- 已 push 到 GitHub: hanxiao199001/wildtrip，commit 58591d8

### 下次继续的优化方向
**目标：完全去掉粘贴步骤，点击直接落地美团搜索结果**

优先做：① 直接传最终美团搜索URL给美团小程序（不经过relay中转HTML）
- relay.py 已有城市ID映射（ci参数）和搜索URL模板
- 小程序端解析relay URL参数 → 自己构建美团搜索URL → 传给美团h5?weburl
- CPS追踪改为后台 wx.request 静默触发（不影响主流程）
- 预计改动：20行左右，效果最直接

其次：② 省掉modal弹窗（直达结果后就不需要弹窗告知"记得粘贴"了）
③ 研究美团小程序原生search路径（比h5 webview快）

## 关键文件
- 小程序：wildtrip-existing/miniprogram/pages/guide-detail/guide-detail.js（isMeituanLink 处理逻辑）
- 后端：wildtrip-existing/backend/api/relay.py（美团城市ID映射 + URL模板）

## 野游记 - 美团跳转终极优化（上午）

### 优化目标
从 **4步**（点击 → modal → 复制 → 粘贴）优化到 **1步**（点击 → 直达搜索结果）

### 实现方案
1. **添加美团城市ID映射**（从relay.py移植）
   - 支持50+城市的ci参数精准定位
   - 兜底模糊匹配（去"市"/"区"字符）

2. **小程序端解析relay URL**
   - 提取 keyword/city/type 参数
   - 直接构建最终美团搜索URL：`https://i.meituan.com/search?keyword=xxx&ci=8`

3. **CPS追踪后台化**
   - 用 `wx.request` 静默触发relay?direct=1
   - 不阻塞主流程，失败也不影响跳转

4. **去掉modal和剪贴板**
   - 点击即跳转，无需确认
   - 直接打开美团搜索结果页

### 代码变更
- 文件：`miniprogram/pages/guide-detail/guide-detail.js`
- 新增：城市ID映射、URL解析、URL构建三个辅助函数
- 重构：isMeituanLink 处理逻辑（95+行 → 30行，更简洁）
- Commit: c01b414

### 待验证
- wx.request 是否受小程序域名白名单限制（如果报错需要在小程序后台加域名）
- 美团h5页面是否正确处理ci参数（可能需要抓包验证）

### 下一步优化（可选）
- 研究美团小程序原生search页路径（比h5更快）
- 添加加载动画（防止用户点击后无反馈）
